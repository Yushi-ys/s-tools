import React from "react";
import { Menu } from "antd";


const Menus: React.FC = () => {
    return <div>
        <Menu mode="inline" defaultSelectedKeys={['4']} items={[]} />
    </div>
}

export default Menus;